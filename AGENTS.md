# AGENTS.md

이 저장소에서 작업하는 모든 에이전트는 아래 지침을 따른다.

## 작업 범위

- 사용자가 요청한 범위와 직접 관련된 변경만 수행한다.
- 요청과 무관한 리팩터링이나 정리는 현재 작업에 섞지 않는다. 추가 작업이 필요하면 별도 작업으로 제안한다.

## 커밋 원칙

- 여러 변경을 마지막에 한 번에 커밋하지 않는다.
- 작업을 가능한 한 작은 **논리적 변경 단위**로 나누고, 각 단위가 끝나는 즉시 커밋한다.
- 하나의 커밋은 한 문장으로 설명할 수 있는 하나의 목적만 가져야 한다.
- 서로 독립적으로 되돌릴 수 있는 변경은 반드시 별도 커밋으로 분리한다.
- 파일 수가 적다는 이유만으로 관련 없는 변경을 같은 커밋에 넣지 않는다.
- 같은 파일 안의 변경도 목적이 다르면 hunk 단위로 나누어 별도 커밋한다.
- 리팩터링, 동작 변경, 버그 수정, 기능 추가, 테스트, 문서, 포맷팅, 의존성 변경, 설정 변경은 서로 독립적인 경우 각각 별도 커밋한다.
- 작업 중 발견한 부수적인 문제를 고칠 경우 원래 작업과 섞지 말고 별도 커밋한다.

## 커밋 분리 기준

다음 질문 중 하나라도 `예`라면 별도 커밋으로 분리한다.

- 이 변경만 독립적으로 리뷰할 수 있는가?
- 이 변경만 독립적으로 되돌릴 수 있는가?
- 이 변경의 목적을 별도의 커밋 메시지로 자연스럽게 설명할 수 있는가?
- 다른 종류의 변경인가? 예: 리팩터링과 기능 변경, 코드와 문서, 설정과 로직
- 다른 컴포넌트나 패키지에 대한 변경인가?
- 후속 변경이 없어도 자체적으로 의미가 있는가?

커밋 순서는 가능한 경우 다음과 같이 의존 관계가 낮은 변경부터 구성한다.

1. 기계적인 이름 변경이나 코드 이동
2. 동작을 바꾸지 않는 리팩터링
3. 기반 타입, 인터페이스, 유틸리티 추가
4. 기능 또는 버그 수정
5. 독립적인 테스트 보강
6. 문서 및 운영 설정 변경

단, 테스트가 특정 동작 변경의 명세이자 필수 검증인 경우에는 구현과 같은 커밋에 포함할 수 있다. 마이그레이션과 이를 반드시 필요로 하는 코드처럼 따로 커밋하면 저장소가 깨지는 변경도 하나의 원자적 커밋으로 묶는다. 커밋 수를 늘리기 위해 빌드 불가, 테스트 실패, 또는 의미 없는 중간 상태를 만들지 않는다.

## 작업 절차

1. 작업 시작 전에 `git status --short`와 현재 diff를 확인한다.
2. 개발을 시작하기 전에 해당 작업을 위한 새로운 브랜치를 생성하고, 생성한 브랜치에서만 작업한다.
3. 구현 전에 예상되는 논리적 변경 단위를 나누고 커밋 경계를 정한다.
4. 하나의 단위를 구현하고 관련 검증을 실행한다.
5. `git diff`로 전체 변경을 확인한다.
6. `git add <구체적인 경로>` 또는 hunk 단위 스테이징으로 해당 단위만 올린다.
7. `git diff --cached`를 확인해 현재 커밋 목적과 무관한 변경이 없는지 검토한다.
8. 검증 가능한 상태라면 즉시 커밋한 뒤 다음 단위로 넘어간다.
9. 작업 완료 후 커밋 목록을 확인해 각 커밋이 독립적인 목적을 갖는지 다시 검토한다.
10. 자동 테스트와 실제 구동·사용 검증을 완료하고 PR에 검증 증거를 기록한다. PR을 생성한 뒤 최신 커밋의 필수 CI와 리뷰 상태를 확인한다.
11. 사용자가 머지를 위임한 작업은 모든 병합 조건이 충족되면 에이전트가 직접 머지한다. 머지 권한이 위임되지 않은 작업은 PR 제출에서 멈춘다.
12. PR 제출 또는 머지 후 `main`으로 전환한다. 머지한 경우 `git pull --ff-only`로 동기화하고 실제 머지 결과를 확인한 뒤 다음 작업 브랜치를 생성한다.

## 자율 개발 반복 절차

- 사용자가 MVP까지 지속 개발을 요청하면 문서의 완료 기준을 작업 목록으로 만들고 의존성 순서대로 구현·검증·수정·PR·머지를 반복한다. PR 하나를 올린 것만으로 전체 목표를 완료 처리하지 않는다.
- 기능 추가와 사용성 개선은 MVP 요구사항 또는 실제 사용 중 재현한 문제에 근거한다. 로드맵 전체를 임의로 MVP에 포함하거나 완료 기준을 낮추지 않는다.
- 이번 MVP 개발에는 사용자가 테스트, 실제 사용 검증, 버그 수정, 커밋, PR 생성과 조건을 충족한 머지를 위임했다. 이 위임은 운영 배포, 유료 인프라 생성, 비밀정보 공개, 브랜치 보호 우회 또는 제품 내부 승인 게이트 제거를 허용하지 않는다.
- 사용자에게 매번 수동 확인을 요구하지 않는다. 다만 필수 외부 환경·권한이 없거나 데이터 삭제·비용·제품 범위에 새 결정이 필요하면 정확한 차단 사유와 필요한 입력을 보고한다. 모의 검증을 실제 환경 검증으로 표현하지 않는다.
- 남은 MVP 항목, 검증 결과, PR·머지 상태와 외부 환경 의존 항목을 작업 기록에 갱신하여 다음 세션에서도 이어갈 수 있게 한다.

## 실제 구동 및 사용 검증

- 자동 테스트 통과만으로 기능을 완료 처리하지 않는다. 격리된 로컬 또는 승인된 테스트 환경에서 관련 서비스를 실제로 실행하고 대표 사용자 여정을 직접 수행한다.
- Web 변경은 실제 브라우저에서 생성·조회·피드백·승인처럼 영향을 받는 흐름, 빈 상태·로딩·오류·권한 거부, 한국어·영어, 데스크톱·좁은 화면을 확인한다. 키보드 탐색, 포커스 표시, 레이블, 대비, 가로 넘침도 검사한다.
- Browser/Computer-use 도구로 최종 화면과 상호작용 결과를 직접 확인한다. UI 변경은 변경 전후 스크린샷을 PR에 첨부하고, 콘솔·네트워크 오류를 점검한다. 네이티브 창·입력·Console 기능을 바꾸면 실제 데스크톱 조작도 검증한다.
- 반복 가능한 핵심 여정은 자동 E2E 테스트로 남긴다. 화면 확인과 자동 E2E는 서로 대체하지 않는다. 실패하면 원인을 수정하고 영향을 받는 테스트와 여정을 다시 실행한다.
- 증거에는 검증한 커밋, 명령·환경, 기대 결과와 관찰 결과를 기록한다. 최종 변경 이전 증거만으로 완료하거나 토큰·개인정보가 담긴 화면을 업로드하지 않는다.
- 테스트 데이터·브라우저 세션·포트는 작업별로 격리하고 직접 만든 프로세스·자원만 정리한다. 사용자 데이터나 실행 중인 다른 앱을 초기화하지 않는다.
- 문서 전용 등 구동 대상이 없는 변경은 해당 없음과 이유를 명시한다. 필수 환경이 없어 검증하지 못한 기능 PR은 Draft로 유지하고 머지하지 않는다.

## UI/UX 원칙

- 화면은 운영 대시보드의 정보 구조를 따른다. 큰 소개문보다 작업 현황, 주의가 필요한 상태, 다음 행동을 먼저 보여준다.
- 일관된 탐색·간격·타이포그래피·상태 색을 사용하고, 상태를 색상만으로 구분하지 않는다. 동작하지 않는 버튼, 가짜 통계·실시간 표시, 불필요한 장식은 추가하지 않는다.
- 핵심 작업은 적은 단계로 수행하게 하고 제출 중 중복 동작 방지, 성공 피드백, 복구 가능한 오류 안내를 제공한다.
- 반응형 레이아웃, 접근성, 긴 제목·저장소 이름, 많은 작업과 빈 목록을 함께 검증한다. UI 문구는 한국어·영어를 동기화한다.

## 빠르고 안전한 CI

- PR과 `main`에 GitHub Actions 검증을 실행한다. 현재 필수 검사 이름과 실행 명령은 Workflow와 개발 가이드에 유지한다.
- 언어별 작업 병렬 실행, 의존성 캐시, 제한된 Timeout, 같은 PR의 이전 실행 취소를 사용한다. 불필요한 다중 버전 Matrix나 중복 전체 빌드는 피한다.
- 경로 기반 생략을 도입할 때 공통 계약·설정 변경의 영향을 포함하고, 필수 검사 누락을 성공으로 간주하지 않는다. 속도를 위해 회귀 테스트·보안 검증을 비활성화하지 않는다.
- Workflow Token은 최소 권한으로 제한하고 외부 Action은 검증된 Commit SHA에 고정한다. 신뢰할 수 없는 PR 코드를 쓰기 Token이나 운영 Secret으로 실행하지 않는다.
- 자동 머지는 검증된 정확한 Head Commit에만 수행한다. 실패·취소·진행 중·누락된 필수 CI, 미해결 리뷰, 병합 충돌이 있으면 원인을 해결한 뒤 재검증한다. `--admin`, 강제 Push 또는 테스트 삭제로 통과시키지 않는다.
- 문서화한 Merge Commit 방식으로 논리적 커밋을 보존한다. 제품이 생성하는 작업 PR의 사용자 승인 정책과 이 저장소의 개발 PR 머지 위임을 혼동하지 않는다.

## 검증 명령

수정한 영역에 해당하는 검증을 반드시 실행한다.

- API: `make test-api`
- Runner: `make test-runner`
- Worker: `make test-worker`
- Gateway: `make test-gateway`
- Web: `make test-web`
- 정적 검사: `make lint`
- 여러 컴포넌트에 걸친 변경: `make test`

실행하지 못한 검증은 완료 보고에 실행하지 못한 명령과 그 사유를 명시한다.

## 테스트 작성 기준

- 버그 수정에는 수정 전 실패하고 수정 후 통과하는 회귀 테스트를 포함한다.
- 새 기능에는 정상 경로와 핵심 실패 경로를 검증하는 테스트를 추가한다.
- 테스트를 통과시키기 위해 검증 수준을 낮추거나 기존 테스트를 임의로 삭제하지 않는다.

## 보안 경계

- 토큰, 비밀번호, 인증서, 실제 환경변수 값을 코드, 로그, 테스트 픽스처, 문서 또는 커밋에 포함하지 않는다.
- 인증, 권한 검사, 네트워크 격리, 승인 게이트를 약화하는 변경은 명시적인 요구 없이 수행하지 않는다.
- `AUTH_MODE=development`를 운영 환경의 기본값으로 설정하지 않는다.

## 컴포넌트 간 계약

- API 스키마, 이벤트 형식, 상태 값, 환경변수 이름처럼 여러 컴포넌트가 공유하는 계약을 변경할 때는 저장소 전체에서 모든 생산자와 소비자를 찾아 함께 갱신한다.
- 하위 호환성을 깨는 변경은 영향 범위와 마이그레이션 방법을 문서화한다.

## 다국어 동기화

- 사용자에게 노출되는 문서나 UI 문구를 변경할 때 한국어와 영어 버전의 동기화 여부를 확인한다.
- 한쪽 언어만 변경해야 한다면 완료 보고에 그 이유를 명시한다.

## 스테이징 및 기존 변경 보호

- 원칙적으로 `git add .`, `git add -A`, 와일드카드 전체 추가를 사용하지 않는다.
- 파일 전체가 하나의 목적에 해당할 때만 파일 단위로 스테이징한다. 목적이 섞여 있으면 hunk 단위로 스테이징한다.
- 사용자가 만든 기존 변경이나 작업 시작 전부터 있던 변경은 수정, 삭제, 복원, 또는 커밋하지 않는다.
- 기존 변경과 현재 작업이 같은 파일에서 겹치면 현재 작업에 해당하는 hunk만 선별한다.
- 생성 파일, 잠금 파일, 포맷터 결과도 실제로 현재 커밋에 필요한지 확인한 뒤 스테이징한다.
- 다른 에이전트가 만든 변경을 자신의 커밋에 포함하지 않는다. 공동 작업으로 불가피하게 포함해야 한다면 커밋 전에 그 이유와 범위를 확인한다.

## 의존성 추가 기준

- 새 의존성을 추가하기 전에 표준 라이브러리나 기존 의존성으로 해결할 수 있는지 확인한다.
- 새 의존성이 필요하면 그 목적과 선택 이유를 완료 보고에 명시한다.

## 커밋 품질

- 각 커밋은 가능한 범위에서 빌드되고 관련 테스트를 통과해야 한다.
- 커밋 메시지의 제목과 본문은 한국어로 작성한다. Conventional Commits의 타입·스코프처럼 프로젝트 규약에서 정한 접두사와 코드 식별자는 예외로 한다.
- 커밋 제목은 `<prefix>: <한국어 설명>` 형식으로 작성해 변경 종류를 바로 알 수 있게 한다.
  - `feat`: 새 기능
  - `fix`: 버그 수정
  - `refactor`: 동작을 바꾸지 않는 코드 구조 개선
  - `test`: 테스트 추가 또는 수정
  - `docs`: 문서 변경
  - `style`: 포맷팅처럼 동작에 영향을 주지 않는 변경
  - `perf`: 성능 개선
  - `build`: 빌드 시스템 또는 의존성 변경
  - `ci`: CI 설정 변경
  - `chore`: 위 분류에 해당하지 않는 유지보수 작업
- 하나의 커밋에는 가장 적합한 접두사 하나만 사용한다.
- 커밋 메시지는 변경의 목적을 구체적으로 설명한다.
- `update`, `changes`, `fix stuff`처럼 범위를 알 수 없는 메시지를 사용하지 않는다.
- 서로 다른 목적을 `and`로 연결해야 하는 커밋 메시지라면 커밋을 더 나눌 수 있는지 먼저 검토한다.
- 포맷팅만 발생한 변경은 동작 변경과 분리한다. 단, 수정한 줄에 자동으로 적용된 최소 포맷팅은 해당 변경에 포함할 수 있다.
- 의존성 선언과 그 결과로 갱신된 잠금 파일은 같은 의존성 변경 커밋에 포함한다.

## Pull Request 원칙

- 하나의 PR은 하나의 명확한 목적만 가진다.
- 서로 독립적으로 배포하거나 되돌릴 수 있는 변경은 별도 PR로 분리한다.
- 큰 작업은 리뷰 가능한 크기의 선행·후속 PR로 나눈다.
- PR 안에서도 논리적으로 분리된 커밋 기록을 유지한다.
- 관련 없는 리팩터링, 포맷팅, 문서 변경을 기능 PR에 섞지 않는다.
- 명시적인 요청 없이 커밋을 squash하거나 의미 있는 커밋 기록을 합치지 않는다.

## PR 제목

- PR 제목은 `<prefix>: <한국어 설명>` 형식으로 작성한다.
- 커밋 메시지와 동일한 접두사 체계를 사용한다.
- 구현 방법보다 사용자 또는 시스템 관점의 결과를 설명한다.
- 여러 목적을 `및`이나 `and`로 연결해야 한다면 PR 분리를 먼저 검토한다.

예시:

- `feat: 작업 실행 상태 실시간 표시 추가`
- `fix: 만료된 Worker 임대가 해제되지 않는 문제 수정`
- `docs: 운영 환경 배포 절차 보강`

## PR 본문

PR 본문에는 다음 내용을 포함한다.

### 변경 목적

- 해결하려는 문제와 변경이 필요한 이유를 설명한다.
- 관련 이슈가 있으면 연결한다.

### 주요 변경 사항

- 리뷰어가 확인해야 할 핵심 변경을 논리적 단위로 정리한다.
- 단순 파일 목록 대신 동작과 구조의 변화를 설명한다.

### 검증

- 실행한 테스트와 정적 검사 명령 및 결과를 기록한다.
- 실행하지 못한 검증은 해당 명령과 사유를 설명한다.

### 영향 및 위험

- 영향을 받는 컴포넌트, API, 설정, 데이터, 배포 절차를 명시한다.
- 하위 호환성 문제, 마이그레이션 필요 여부, 알려진 제한사항을 기록한다.
- 위험이 있는 변경은 롤백 방법을 함께 작성한다.

## 변경 유형별 PR 자료

- UI 변경에는 변경 전후 스크린샷 또는 영상을 첨부한다.
- API 변경에는 요청·응답 예시와 호환성 영향을 작성한다.
- 설정 변경에는 새 환경변수, 기본값, 운영 적용 방법을 작성한다.
- 데이터 구조 변경에는 마이그레이션과 롤백 절차를 작성한다.
- 보안 관련 변경에는 민감한 세부정보를 노출하지 않는 범위에서 위협과 대응 방식을 설명한다.

## PR 제출 전 확인

- 제출 전에 전체 diff와 커밋 목록을 직접 검토한다.
- 임시 코드, 디버그 출력, 불필요한 파일, 비밀정보가 없는지 확인한다.
- 관련 테스트와 정적 검사를 통과한 뒤 리뷰 준비 상태로 전환한다.
- 아직 검증되지 않았거나 후속 작업이 필요한 PR은 Draft로 생성한다.
- 리뷰가 시작된 뒤 커밋 기록을 재작성하거나 강제 푸시해야 한다면 변경 이유를 리뷰어에게 알린다.
- 리뷰 의견 반영으로 범위가 크게 달라지면 PR 설명과 검증 결과도 갱신한다.

## PR 병합 조건

- 필수 CI 검사와 관련 테스트가 통과해야 한다.
- 해결되지 않은 리뷰 의견이 없어야 한다.
- 문서, 마이그레이션, 다국어 콘텐츠처럼 필요한 후속 변경이 포함되어야 한다.
- PR 설명이 최종 변경 내용과 일치해야 한다.
- 실제 구동·사용 검증 증거가 최종 코드에 유효해야 한다. GitHub에 표시된 최신 Head SHA와 검증한 SHA가 같아야 한다.

## 완료 조건

- 코드 작성만으로 작업을 완료한 것으로 간주하지 않는다.
- 관련 테스트, 정적 검사, diff 검토, 불필요한 파일 포함 여부 확인을 마친 뒤 완료한다.
- 임시 코드, 디버그 출력, 주석 처리된 코드, 근거 없는 TODO를 남기지 않는다.

## 완료 보고

작업 결과를 보고할 때 다음 내용을 함께 제공한다.

- 생성한 커밋 목록과 각 커밋의 목적
- 각 커밋 또는 전체 작업에 대해 실행한 검증
- 커밋하지 않고 남겨 둔 변경이 있다면 그 목록과 이유
- 실제 사용 검증 결과와 증거, PR URL·CI 상태·머지 여부, 남은 MVP 항목과 차단 사유

명시적으로 커밋하지 말라는 요청을 받은 경우에는 커밋을 생성하지 않는다. 그 경우에도 변경 내용을 위 기준에 따라 논리적 단위로 유지하고, 어떤 커밋들로 나눌 수 있는지 완료 보고에 제안한다.
