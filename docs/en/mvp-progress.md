# MVP progress — 2026-09-11

[한국어](../ko/mvp-progress.md) | English · [Summary](roadmap-summary.md) · [Detailed criteria](roadmap-detailed.md)

## Baseline and percentage

Product baseline: `main` at `95aac9d81d4ea5ed7ffb083995dd56ead157c3ac` (merged [PR #59](https://github.com/haesookimDev/dev-agent/pull/59)). The lease-reconciliation API was verified at `f5d9da32c6500c413c4340d914997cda53b4e479`. Follow-up code `b9fdd484f4d7777345e89694c0c2a00b42f87816` for unmerged Worker lifecycle PR #58 verifies terminal-lease recovery with a real VM, new Worker process, API and PostgreSQL. Full Executor and nonterminal recovery remain. This snapshot compares code, tests and actual runtime evidence; draft branches and plans do not count as complete.

Use the seven numbered steps in [the next release](roadmap-summary.md) as a fixed denominator. **Verified complete: 1/7 = 14.3%**; partial: 3/7; incomplete: 3/7. Partial implementation receives no arbitrary fractional credit. This measures verified release stages, not code volume, effort spent or remaining schedule. Current evidence does not support a precise engineering-effort completion percentage.

## Status against completion criteria

| Stage | Assessment and inspected evidence | Remaining for completion |
| --- | --- | --- |
| 1. Versioned DB migration | **Complete.** Alembic Head `20260909_0011`, PostgreSQL migration lock, default `validate`, empty/legacy adoption, safe downgrade, failed-chain rollback and actual restore regressions. [Operations](operations.md), [migration tests](../../apps/api/tests/test_migrations.py), [atomicity](../../apps/api/tests/test_migration_atomicity.py) | Every future schema change must pass the same gates. This does not authorize arbitrary production database cutover. |
| 2. OIDC, organization/repository authorization and audit | **Partial.** OIDC/RBAC and append-only feedback/approval/queued cancellation/delivery audit exist. [Operations](operations.md), [IAM tests](../../apps/api/tests/test_iam.py), [audit](control-action-audit.md) | OIDC Preview Grant integration and actual TLS-boundary validation; running-work administrative cancellation with confirmed VM shutdown/cleanup. |
| 3. Worker secrets and quarantine | **Partial.** File provider, individual issuance/overlap rotation/revocation, control-plane quarantine and Runner/API/Worker diagnostic protection. [Credentials](worker-credentials.md), [quarantine](worker-quarantine.md), [latest diagnostics](worker-private-diagnostics.md) | Physical Host/VM/network and established-connection isolation; comprehensive event/artifact/crash-dump/cloud-init non-disclosure and retention policy. Limited redaction is not general secret scanning. |
| 4. Observation, recovery and retention foundation | **Partial.** Correlation/metrics/DB readiness/alerts, real PostgreSQL restore, ordinary-artifact backup and scheduled cleanup. The merged [terminal-lease reconciliation API](worker-lease-reconciliation.md) passed DB/HTTP concurrency checks; unmerged [Worker restart integration](worker-restart-recovery.md) verified actual VM cleanup and a single return/audit. [Observation](execution-monitoring.md), [restore](postgres-restore.md), [scheduler](scheduled-retention.md) | Nonterminal/lost-Claim recovery, external-dependency readiness, progress-based stall detection/operational dashboard, other retention/janitors, physically safe running cancellation/retry/forced release, external-store and operational recovery verification. |
| 5. Golden image and real libvirt execution | **Incomplete.** Draft image PR #55 has actual ARM64 build/two-boot evidence. Unmerged [Worker lifecycle](worker-lifecycle.md) verifies durable ownership, cancellation cleanup, ACLs, two real blank VM cases and terminal-lease recovery by a new Worker/real API. | Actual amd64 image checks and remaining GUI gates; complete executor Golden Image/Runner integration and normal OS shutdown; nonterminal/complete orphan recovery, per-work networks and physical time-budget enforcement. |
| 6. WireGuard previews and console ownership | **Incomplete.** The [gateway](../../apps/gateway/main.go) returns 503 without production authentication and only forwards a read-only console header. | Actual WireGuard routing, wildcard TLS, organization/work/expiry/target validation, noVNC input filtering, stopping agent input, versioned return and timeout recovery. Headers do not prove input blocking. |
| 7. Real-host concurrent two-work acceptance | **Incomplete.** Current Mock/HTTP/Chromium regressions are not substitute evidence. | Two different repositories must concurrently clone→analyze→implement→browser-verify→receive feedback→reverify→approve→deliver→clean up, recovering from Worker restart/network interruption with isolated displays/input/profiles/networks/disks/credentials. |

These stages summarize detailed P0/P1 requirements; they do not waive [security invariants](security.md), approval policy or actual-use gates. Do not arbitrarily add all P2/GitLab/advanced routing/autonomous discovery to this MVP or remove unfinished P0/P1 conditions.

## Development foundation already built

- [Task-centered UI/UX](dashboard-verification.md): dashboard navigation/search/status filters/responsiveness/error recovery, followed by feedback/evidence/budget-approval improvements. This is separate from completing two real KVM jobs.
- [Development instructions](../../AGENTS.md): branch before implementation, focused Korean commits, automated and actual-use verification, evidence-backed PRs, exact-head checks, normal merge commits and `main` verification. Production deployment, paid resources and protection bypass are not authorized.
- [GitHub CI](../../.github/workflows/ci.yml): two API partitions, subsequent PostgreSQL/Runner gate, Go and Web/Chromium. Pinned action SHAs, least privilege, caching, 8-minute job limits, older-run cancellation per PR and evidence retention remain. [Partition policy](ci-partitions.md)
- [PR #53 CI](https://github.com/haesookimDev/dev-agent/actions/runs/34422241723) passed first attempt in 4m56s at exact Head `9d9b35f`: API 503/663, separate PostgreSQL groups 16/23/38/8/12/6/9, Runner 45, Web 125/Chromium 41, Go and static checks. API-stage PostgreSQL skips are covered by subsequent database checks.

Do not double-count this foundation toward the seven product release stages. The actual Worker/production-Web checks and native-focus limitation are separated in the [diagnostic verification record](worker-private-diagnostics.md).

## Next order and external dependencies

Following the user's Mac-local development approval on 2026-09-10, **an actual alternative to the missing separate Linux machine is verified**. The [Mac KVM lab](macos-kvm-lab.md) ran M4 Pro/macOS 15.7.3 → Lima 2.2.0/Ubuntu ARM64 → real KVM Linux boot/clean poweroff (5.790 seconds), confirmed active KVM, cleaned timed-out processes and preserved existing evidence. Host verification does not complete Golden Image/work-VM acceptance, so the numerator remains 1/7. This adds ARM64 local testing; it does not waive existing amd64 product verification.

The earlier host-stage full run with dedicated PostgreSQL passed API 1270/1 skip. On API PR #59's final code, `make test` passed API 1195/152 skips, Runner 45, Web 125/type checking, Go and Lab 18; `make lint` also passed. The ordinary API run skips new recovery checks without the DB environment, but the separate real PostgreSQL run passed 46/46; the combined existing-Worker/recovery CI command passed 62/62. See [API evidence and limits](worker-lease-reconciliation.md). These checks reuse the Python job without a new job or VM build. PR #59 passed [all five CI checks](https://github.com/haesookimDev/dev-agent/actions/runs/34563221182) first attempt in 5m47s at exact Head `e3521e1b78369da0a7bb58f609bc73b67f5b88ba` and was merged normally with no unresolved review. Keep this separate from follow-up Worker-integration evidence.

Follow-up Worker `make test` passed API 1195/153 skips (155.48 seconds), Runner 45, Web 125/type checking, Worker/Gateway and Lab 18; `make lint` and focused race checks also passed. The new real VM/API/PostgreSQL opt-in check passed separately: 1 test/22.88 seconds. The actual Worker's commit/`vcs.modified=false`, single release event/audit and cleaned temporary resources are recorded in [restart evidence](worker-restart-recovery.md). This is not full Executor SIGKILL, OS/Runner, GUI or concurrent-two-work proof. PR #58 records its final-head CI and Draft status separately.

1. **Prioritize the P1 execution path:** reproducible golden image → persistent VM lifecycle/isolation → enforced preview/console boundaries → actual two-work/failure-recovery acceptance. Feature PRs lacking necessary real-environment verification remain Draft.
2. Complete remaining P0 secret/observation/retention/administrative controls in connection with that execution path. Repeated easily tested peripheral improvements do not substitute for P1 completion.
3. [Draft PR #55](https://github.com/haesookimDev/dev-agent/pull/55), Head `b865954101a4becd37d23f4aadf39a9d9795585f`: actual ARM64 desktop image creation, 8/8 checks on each of two standard boots, unchanged base image, and actual screen/keyboard-input evidence. [Immutable evidence](https://github.com/haesookimDev/dev-agent/blob/b865954101a4becd37d23f4aadf39a9d9795585f/docs/en/golden-image-inputs.md); [all five CI checks passed](https://github.com/haesookimDev/dev-agent/actions/runs/34556143436) first attempt in 7m43s. Full GUI journeys on the final candidate, real amd64 verification and Worker integration remain incomplete, so it stays Draft.
4. Previous Head `2780f4d3d3f9ba6dc6647293c61fef64aff88403` of [Draft PR #58](https://github.com/haesookimDev/dev-agent/pull/58): durable run records, cleanup-before-release, and two actual blank ARM KVM cases (terminal/cancelled) verifying stop→undefine→file removal→mock API acknowledgement. [Immutable evidence](https://github.com/haesookimDev/dev-agent/blob/2780f4d3d3f9ba6dc6647293c61fef64aff88403/docs/en/worker-lifecycle.md); [previous five CI checks passed](https://github.com/haesookimDev/dev-agent/actions/runs/34560620314) first attempt in about 5m20s. This follow-up adds schema-2 lease binding and actual new Worker/VM/API recovery. Full Golden Image/Runner execution, owned ARM firmware and per-work networking/time budgets remain. Sequential blank VMs are not concurrent two-work acceptance.
5. PR #58's latest head `0274718914ccf193fc7caf51ce561b08a242a61c` passed [all five CI checks](https://github.com/haesookimDev/dev-agent/actions/runs/34566826882) on the first attempt in 6 minutes 1 second, with no unresolved reviews, but remains Draft. Follow-up [ARM firmware ownership](worker-firmware.md) source `dafc0dab677756887b2707871e83deb3ab1748da` passed two actual Executor creation/UEFI/cleanup cases in 48.90 seconds. Existing amd64 boot selection is preserved; the documented fixture removes NIC/graphics and uses blank disks/synthetic HTTP. This unmerged evidence does not complete the full Executor or a release stage.
6. ARM firmware [Draft PR #60](https://github.com/haesookimDev/dev-agent/pull/60), head `31793bcb9d91ac8d93b7b7c1dde7109290670d0e`, also passed [all five CI checks](https://github.com/haesookimDev/dev-agent/actions/runs/34567965046) first attempt in 5 minutes 17 seconds. Separate [VM command limits](worker-command-limits.md) source `ae81537ae8b6b155614ba123a8f4df02dbdbc253` verifies a 45-second per-command limit, descendant termination on cancellation and cancellation after actual VM creation (three actual cases/70.64 seconds). This is distinct from total-work time-budget enforcement; all remain unmerged.
7. Command-limit [Draft PR #61](https://github.com/haesookimDev/dev-agent/pull/61), head `9d055382e7d1bcd428b2bbccea7daea66c56b88d`, passed [all five CI checks](https://github.com/haesookimDev/dev-agent/actions/runs/34568940739) first attempt in 5 minutes 45 seconds. Follow-up [per-work network foundation](worker-network-foundation.md) source `50ada438156ea596cb07d317911bf12f715e194b` adds collision-rejecting address allocation, network/deny-all filter XML and installed-libvirt schema validation on actual Linux. It is not connected to the Executor or actual packet verification, so it adds no isolation or release-stage completion credit.
8. Next, connect durable network ownership, safe creation/cleanup, host boundaries and egress policy, then verify the full Executor's Golden Image/Runner path and total VM time budgets. Separately verify nonterminal recovery policy and lost Claim responses. Preview in [Draft PR #21](https://github.com/haesookimDev/dev-agent/pull/21) is still absent from `main`. Do not count complete or force-merge before per-work networking/TLS/console/concurrent-two-work proof. Do not discover or use ambient production hosts or credentials without approval.

## Updating this record

For subsequent completion reports, update the baseline SHA, PR/CI/actual-use evidence and remaining conditions. Increment the numerator only when every condition of a stage is proven. Regressions reopen its status with a reason. Do not change the denominator/scope to inflate the percentage; record user-approved scope changes and differences from the previous baseline. Update Korean and English together.
